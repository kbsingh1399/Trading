Option 1: Deliver the fully expanded, production-ready implementation directly in chat.

================================================================================
MANDATE FOR FULL MULTI-POD PRODUCTION REBUILD & KUSTO INGESTION
================================================================================

Opus, your architecture is approved, but you must immediately PURGE all mock data placeholders (np.random.normal) and daily frequency simplifications. 

Our production universe is strictly 15-minute institutional bars across the 18 Binance USDT-M perpetuals (3.47M candles from 2020 to 2026), governed by our canonical schema (Binance_Master_15min and Binance_Footprint_15min).

Deliver the complete, unabridged, copy-paste ready code files with zero placeholders:

--------------------------------------------------------------------------------
FILE 1: Engine/pipeline/kusto_client.py (Production Kusto / ADX Data Ingestor)
--------------------------------------------------------------------------------
1. Azure Data Explorer (ADX) Connector:
   - Provide a complete query engine using azure-kusto-data (or standard HTTPS REST API endpoint with urllib/requests).
   - Tables: Binance_Master_15min (58 features) and Binance_Footprint_15min (13 tick ladder features).
2. 500,000 Row Guardrail & Window Chunking:
   - Automatically split queries by symbol and time chunks (e.g. 1-month slices) to prevent Kusto truncation errors.
   - Enforce causal 72-hour purge: t_purge = t_start - 72h.
3. Local Cache & Fallback:
   - Cache downloaded windows as local Parquet (caching to binance_backtesting_data/).
   - If offline or credentials omitted, seamlessly load the local Parquet files.

--------------------------------------------------------------------------------
FILE 2: Engine/strategy/multi_pod_portfolio.py (Core Pod Engine & Risk Allocator)
--------------------------------------------------------------------------------
1. Implement the 5 Pods with True Quantitative Math (No Placeholders):
   - Pod 1: XMOMPod (Volatility-Adjusted Momentum):
     Compute 40-period return normalized by ATR(14) or realized rolling volatility to prevent high-beta meme tokens from skewing the book.
   - Pod 2: FundingCarryPod:
     Ingest real funding_rate_pct (settling every 8h at 00:00, 08:00, 16:00 UTC). Long negative funding, short positive funding with market-neutral dollar balancing.
   - Pod 3: LeadLagPod:
     Estimate rolling 96-bar (24h) beta between altcoins and leaders (BTCUSDT, ETHUSDT). Trade the cross-sectional return residual when altcoin lags the leader impulse.
   - Pod 4: PCAResidualPod (Statistical Arbitrage):
     Rolling 60-bar PCA on standardized 15m returns. Extract first 2 principal components (Market and Sector beta), compute idiosyncratic residual return, and fade extreme Z-score deviations (> 1.8 sigma).
   - Pod 5: VolReversalPod (Microstructure Orderflow Fade):
     Ingest orderflow features: long_liq_zs, short_liq_zs, and zc_div. Fade liquidation cascades and footprint volume exhaustion at Value Area extremes (session_vah / session_val).
2. Institutional Risk & Turnover Layer:
   - Initial Capital: 5,000.00 USD.
   - Portfolio Neutrality: sum(weights) = 0 (dollar-neutral), gross exposure = 1.0.
   - Real Turnover Friction: friction = sum(|w_t - w_{t-1}|) * 41 bps (8 bps fee + 10 bps entry slippage + 15 bps stop slippage).
   - Mark-to-Market Circuit Breaker: Hard stop if drawdown exceeds 4.5% (225.00 USD).
   - Max 2 concurrent fills across all 18 symbols.

--------------------------------------------------------------------------------
FILE 3: scratch/run_20_oos_multi_pod.py (20 OOS Walk-Forward Runner)
--------------------------------------------------------------------------------
1. Ingest all 20 non-overlapping quarterly windows (2021 to 2026) defined in Engine/oos_windows_20.json.
2. Execute the multi-pod simulation bar-by-bar with causal next-bar entry (opens[t+1]).
3. Print the complete Markdown scorecard table with all 20 rows showing:
   Window ID, Regime Name, Net ROI %, Max Drawdown %, Completed Trades, Win Rate %, Pass Status (Yes/No).
4. Verify that all 20 windows pass the institutional criteria:
   Net ROI >= +10.0%, Max DD <= 5.0%, Trades >= 15, Win Rate >= 40.0%.

Deliver the full, unabridged code blocks now in chat. No zip files, no sandbox links, no pseudo-code.
