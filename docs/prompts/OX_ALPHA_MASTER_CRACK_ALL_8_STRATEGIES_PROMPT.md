# OX ALPHA MASTER DIRECTIVE: INSTITUTIONAL ML CRACKER FOR ALL 8 PERPETUAL STRATEGIES

> **ROLE:** Senior Institutional Quantitative Researcher & Lead High-Frequency Trading Systems Architect (Ox Alpha).  
> **DIRECTIVE:** Zero fluff, zero conversational filler, maximum mathematical precision. Provide production-grade, fully vectorized, zero-lookahead Python code and quantitative architectures.

---

## 1. MISSION STATEMENT & OBJECTIVE
We are subjecting our quantitative perpetual trading engine to a rigorous **20 Non-Overlapping Out-Of-Sample (OOS) Windows protocol (2021–2026)** across **18 Binance USDT-M Perpetual contracts** (3.47 million 15-minute candles, 0 nulls, strictly monotonic timestamps).

You must provide the definitive quantitative and Machine Learning solution to crack and upgrade **ALL 8 STRATEGY ENGINES** so that they simultaneously satisfy our settled institutional benchmarks.

### Canonical Target Pass Criteria (Per Window):
Source: [`target_oos_criteria.json`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/target_oos_criteria.json)
- **Minimum ROI:** `> 20.0%`
- **Maximum Drawdown:** `< 5.0%` (Hard portfolio drawdown stop at 4.5% / $225 on $5,000 capital)
- **Minimum Win Rate:** `> 40.0%`
- **Exit Target:** Strictly `+2.5R` (Purged legacy 5.0R fantasy that caused 85.8% of winners to retrace into full stop-outs)
- **Minimum Trades:** `>= 6` trades per window
- **20 OOS Windows Reference:** [`oos_windows_20.json`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/oos_windows_20.json)

---

## 2. REPOSITORY ASSETS & RAW CODE CONTRACTS
Inspect the live codebase via these direct raw GitHub references:

### Core Infrastructure & Execution:
- **Execution Kernel:** [`Engine/core/execution_kernel.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/core/execution_kernel.py)
- **Data Schema:** [`Engine/core/schema.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/core/schema.py)

### The 8 Strategies to Crack:
1. **S1 Liquidation Cascade:** [`Engine/strategy/s1_liquidation_cascade.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/s1_liquidation_cascade.py)
2. **S2 Institutional ML:** [`Engine/strategy/s2_institutional_ml.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/s2_institutional_ml.py)
3. **SMC Edgeful:** [`Engine/strategy/smc_edgeful.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_edgeful.py)
4. **SMC Kane:** [`Engine/strategy/smc_kane.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_kane.py)
5. **SMC Marci:** [`Engine/strategy/smc_marci.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_marci.py)
6. **SMC Marco:** [`Engine/strategy/smc_marco.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_marco.py)
7. **SMC Mayne:** [`Engine/strategy/smc_mayne.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_mayne.py)
8. **SMC Usman Noah:** [`Engine/strategy/smc_usman_noah.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/strategy/smc_usman_noah.py)

---

## 3. THREE MANDATORY DELIVERABLES

### DELIVERABLE 1: SURGICAL FIX FOR EXECUTION KERNEL LOOKAHEAD BIAS
In [`Engine/core/execution_kernel.py`](https://raw.githubusercontent.com/kbsingh1399/Trading/main/Engine/core/execution_kernel.py), the current simulation loop contains two lethal flaws:
1. Retroactive execution: Entries triggered on bar $j$ close are filled at bar $j$ open price.
2. Skipped entry-bar exit: Stop-loss and take-profit checks are bypassed on the trade's entry bar.

**Requirement:** Output the exact drop-in replacement execution loop method enforcing:
- Strict causal entry at **`Open` of bar $j+1$** following signal on bar $j$.
- Immediate intrabar evaluation of Stop-Loss on bar $j+1$ (`Low <= stop_loss`), with conservative stop-first resolution if both stop and target are touched.
- Microstructure ratchets armed at bar $j$ close take effect at bar $j+1$ open only.

---

### DELIVERABLE 2: WORLD-CLASS ML META-LABELING ARCHITECTURE
Do not propose naive end-to-end black-box classifiers that suffer from severe class imbalance and noise overfitting. Implement the institutional state-of-the-art: **Marcos Lopez de Prado Secondary Meta-Labeling (Advances in Financial Machine Learning)**.

#### Mathematical Architecture:
1. **Primary Model ($y_{\text{prim}} \in \{0, 1\}$):**
   - Candidate directional setup generated by strategy logic (e.g. Liquidation Z-score $> 1.8$ for S1, Turtle Soup liquidity sweep for Marco, Order Block mitigation for Kane).
2. **Event-Conditioned Sampling:**
   - Discard the $98\%$ non-event bars. Train the ML model strictly on bars where $y_{\text{prim}} == 1$.
3. **Triple-Barrier Labeling Scheme ($y_{\text{meta}} \in \{0, 1\}$):**
   - Upper Profit Barrier: $\text{Entry} + 2.50 \times \text{ATR}$ (or $+2.5R$) $\to$ Label = 1.
   - Lower Stop Barrier: $\text{Entry} - 1.00 \times \text{ATR}$ (or $-1.0R$) $\to$ Label = 0.
   - Vertical Time Barrier: $24$ bars (6 hours on 15m) $\to$ Label = 0 if unrealized PnL $< +0.20R$.
4. **Purged & Embargoed Causal Walk-Forward CV:**
   - Strict $72$-hour purge window ($t_{\text{purge}} = t_{\text{start}} - 72\text{h}$) to eliminate information leakage from overlapping holding periods.
5. **Regularized Shallow GBDT Configuration:**
   - Algorithm: `XGBClassifier` or `LGBMClassifier`.
   - Regularization: `max_depth <= 4`, `reg_alpha >= 1.5`, `reg_lambda >= 3.5`, `subsample=0.8`, `colsample_bytree=0.8`, `learning_rate=0.03`.
6. **Stationary Microstructure Feature Pipeline:**
   - Footprint Delta Ratio (`fp_delta_ratio`), Cumulative Volume Delta divergence (`zc_div`), Liquidation Z-scores (`long_liq_zs`, `short_liq_zs`), RSI-14, VWAP Z-score distance (`dist_to_vwap_zs`), and Volatility scalars (`atr_norm`).

**Requirement:** Provide the complete, self-contained Python class `InstitutionalMetaLabeler` ready for production import.

---

### DELIVERABLE 3: UNIFIED INTEGRATION ACROSS ALL 8 STRATEGIES
Demonstrate the exact integration pattern showing how `InstitutionalMetaLabeler` wraps:
1. The **Quant/Microstructure Models** (`s1_liquidation_cascade.py` and `s2_institutional_ml.py`).
2. The **6 SMC Playbooks** (`smc_mayne.py`, `smc_marco.py`, `smc_kane.py`, `smc_edgeful.py`, `smc_usman_noah.py`, `smc_marci.py`).

Each integrated strategy must enforce:
- Primary rule confluence.
- Meta-model trade confidence filter ($P(\text{success}) \ge \tau$).
- Microstructure Exit Ratchet:
  - $+0.8R$ gain $\to$ Move stop to Entry $+0.15R$ (Breakeven lock).
  - $+1.5R$ gain $\to$ Move stop to Entry $+0.80R$ (Profit lock).
  - Hard Take-Profit at $+2.5R$.
  - Time decay exit if $< +0.2R$ after 24 bars.

---

## ⚡ OUTPUT FORMAT
Provide pure, production-ready Python code blocks:
1. `ExecutionKernel` patched methods.
2. `InstitutionalMetaLabeler` class definition.
3. Strategy patch templates for S1, S2, and the SMC suite.
